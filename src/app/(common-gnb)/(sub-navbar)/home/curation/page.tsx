import { metadataMap } from '@/lib/metadata';

export const metadata = metadataMap.curation;

function Curation() {
  return (
    <main className="typo-title-24-bold-tight py-[400px] flex items-center justify-center">
      곧 오픈할 예정이에요!
    </main>
  );
}

export default Curation;
